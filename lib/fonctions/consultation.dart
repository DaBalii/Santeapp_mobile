import 'package:flutter/material.dart';
class consultFon extends StatelessWidget {
  const consultFon({super.key});

  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: Text('consultation'),
        backgroundColor: Colors.teal,

      ),
    );
  }
}
